%% -*-erlang-*-
[
 {grpcbox, [{client, #{channels => [{default_channel, [{https, "localhost", 8443,
                                                        [
                                                         {keyfile, "../certs/key.pem"},
                                                         {certfile, "../certs/cert.pem"}
                                                        ]}],
                                     #{%% stats_handler => grpcbox_oc_stats_handler
                                      }}]}}
           ]
 }
].
